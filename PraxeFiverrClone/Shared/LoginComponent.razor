@using LoginService;
@inject LoginManager loginManager;

@code {
    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            loginManager.Fetch();
        }
    }
}
